<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Supermarket App</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <%- include("partials/navbar", { user: user }) %>

  <main class="container py-5">
    <div class="row justify-content-center">
      <div class="col-12 col-md-10 col-lg-8">
        <div class="card shadow-sm">
          <div class="card-body text-center">
            <% if (!user) { %>
              <h2 class="card-title mb-3">Welcome to Supermarket App</h2>
              <p class="text-muted mb-4">Please sign in or create an account to start shopping.</p>

              <div class="d-flex flex-column flex-sm-row justify-content-center gap-2">
                <a href="/login" class="btn btn-primary btn-lg">Login</a>
                <a href="/register" class="btn btn-outline-primary btn-lg">Register</a>
              </div>
            <% } else { %>
              <h2 class="card-title mb-2">Welcome back, <%= user.username %>!</h2>
              <p class="text-muted mb-4">You are signed in as <strong><%= user.role %></strong>.</p>

              <div class="d-flex flex-column flex-sm-row justify-content-center gap-2">
                <a href="/shopping" class="btn btn-primary btn-lg">Shop</a>
                <a href="/cart" class="btn btn-outline-primary btn-lg">Cart</a>

                <% if (user.role === 'admin') { %>
                  <a href="/inventory" class="btn btn-warning btn-lg">Inventory</a>
                <% } %>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </main>

  <%- include("partials/footer") %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>