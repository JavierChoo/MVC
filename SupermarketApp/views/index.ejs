<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Supermarket App</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <%- include("partials/navbar", { user: user }) %>

  <main class="container py-5">
    <div class="row justify-content-center">
      <div class="col-12 col-md-10 col-lg-8">
        <div class="card shadow-sm">
          <div class="card-body text-center">
            <% if (user) { %>
              <h2 class="card-title">Welcome back, <%= user.username %>!</h2>
              <p class="text-muted mb-4">You are signed in as <strong><%= user.role %></strong>.</p>
            <% } else { %>
              <h2 class="card-title">Welcome to Supermarket App</h2>
              <p class="text-muted mb-4">Sign in or register to start shopping.</p>
            <% } %>

            <div class="d-flex flex-column flex-sm-row justify-content-center gap-2">
              <a href="/shopping" class="btn btn-primary btn-lg">Shop</a>
              <a href="/cart" class="btn btn-outline-primary btn-lg">Cart</a>

              <% if (!user) { %>
                <a href="/login" class="btn btn-secondary btn-lg">Login</a>
                <a href="/register" class="btn btn-outline-secondary btn-lg">Register</a>
              <% } else if (user.role === 'admin') { %>
                <a href="/inventory" class="btn btn-warning btn-lg">Inventory</a>
                <a href="/logout" class="btn btn-outline-dark btn-lg">Logout</a>
              <% } else { %>
                <a href="/logout" class="btn btn-outline-dark btn-lg">Logout</a>
              <% } %>
            </div>
          </div>
        </div>

        <div class="mt-4 text-center text-muted">
          <small>Quick links: Browse products, manage your cart, and view orders.</small>
        </div>
      </div>
    </div>
  </main>

  <%- include("partials/footer") %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>